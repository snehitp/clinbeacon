import { Component } from '@angular/core';

@Component({
  selector: 'vcf-import',
  template: `
    <h1>{{title}}</h1>
    <div>
      Test
    </div>
    `
})

export class HomeComponent {
  title = 'Home Page';
}